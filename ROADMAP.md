# Secure Archive CMS 개발 로드맵

최종 수정일: 2026-08-25

## 1. 프로젝트 방향

`Secure Archive CMS`는 Spring Boot, React, MySQL 기반의 보안 등급형 내부 문서 관리 플랫폼이다.

- TSP Foundation 세계관은 데모 콘텐츠와 샘플 데이터로 사용한다.
- 인증, 권한, 문서 결재, 감사 이력 구조는 범용 CMS로 설계한다.
- 핵심 API가 안정화되면 기능 하나를 백엔드부터 React 화면까지 세로로 완성한다.
- 모든 권한은 화면 숨김만으로 처리하지 않고 Service에서 다시 검사한다.

## 2. 현재 구현 상태

### 백엔드 완료

- [x] Spring Boot 3, Java 21, MySQL, JPA, Flyway 설정
- [x] JWT 회원가입, 로그인, 내 정보 조회
- [x] BCrypt 비밀번호 해시 처리
- [x] 비활성 계정 로그인 차단
- [x] 공통 API 오류 응답과 입력값 검증
- [x] 사용자, 부서, 부서 계급, 부서 소속 엔티티
- [x] 초기 부서와 부서별 Level-0~5 계급 데이터
- [x] 기본 문서 생성, 상세 조회, 수정, 공개 목록
- [x] 부서 없는 사용자의 미배정 문서 작성
- [x] 실제 소속 부서만 문서 담당 부서로 지정 가능
- [x] 비로그인 사용자의 공개 Level-0 문서 열람
- [x] 인가등급에 따른 공개 문서 열람 제한
- [x] 문서 상태 흐름 `DRAFT -> PENDING_REVIEW -> PUBLISHED`
- [x] 같은 부서 Level-4 이상의 `PENDING_REVIEW` 문서 반려
- [x] 같은 부서 Level-5 이상의 최종 승인
- [x] 상위 전역 역할의 전체 부서 문서 처리
- [x] 핵심 문서 상태 및 권한 테스트

### 프론트엔드 현재 상태

- [x] Vite + TypeScript 프로젝트 생성
- [ ] React 설치 및 설정
- [ ] 실제 애플리케이션 화면
- [ ] 백엔드 API 연결

## 3. 전체 개발 순서

1. Phase 1: 부서 직원 관리 시스템
2. Phase 2: 문서 작업 목록, 검색, 페이지네이션
3. React 시작 지점
4. Phase 3: React MVP 화면
5. Phase 4: 결재 이력과 반려 사유
6. Phase 5: 문서 버전과 보관
7. Phase 6: 추가 문서 양식
8. Phase 7: 첨부파일, 댓글, 알림, 고급 검색
9. Phase 8: 보안 강화, 문서화, 배포

---

## Phase 1. 부서 직원 관리 시스템

다음에 바로 구현할 백엔드 기능이다. 테이블과 Repository는 있지만 관리용 Service와 API가 없다.

### 1-1. 최초 관리자 준비

- [ ] 환경변수 또는 일회성 부트스트랩으로 최초 관리자 생성
- [ ] 실제 관리자 비밀번호를 Flyway나 Git에 저장하지 않기
- [ ] 사용자 상태, 전역 역할, 인가등급 변경 API

예상 API:

```text
PATCH /api/users/{userId}/status
PATCH /api/users/{userId}/role
PATCH /api/users/{userId}/clearance
```

### 1-2. 부서 소속 관리

- [ ] 현재 사용자의 모든 활성 부서 소속 조회
- [ ] 부서의 활성 직원 목록 조회
- [ ] 부서 없는 기존 사용자를 부서에 배정
- [ ] 직원의 부서 계급 변경
- [ ] 삭제 대신 `leftAt`을 기록하여 부서 탈퇴 처리
- [ ] 같은 부서에 활성 소속이 중복 생성되지 않도록 검사
- [ ] Entity와 DB의 `department_rank_id` null 허용 여부 통일

예상 API:

```text
GET   /api/users/me/memberships
GET   /api/departments/{departmentId}/members
POST  /api/departments/{departmentId}/members
PATCH /api/department-memberships/{membershipId}/rank
PATCH /api/department-memberships/{membershipId}/leave
```

### 1-3. 직원 관리 권한

- [ ] 같은 부서 Level-5는 Level-0~4 직원 관리 가능
- [ ] Level-5는 자신과 같은 Level-5 부서장을 임명하거나 해임할 수 없음
- [ ] Level-5는 전역 `UserRole`과 전역 인가등급을 변경할 수 없음
- [ ] 전역 관리자는 부서장 임명과 계정 전체 속성 관리 가능
- [ ] 다른 부서 직원은 관리할 수 없음
- [ ] 탈퇴한 소속은 관리 권한으로 인정하지 않음

### Phase 1 완료 조건

- 부서 없는 Level-0 사용자가 그대로 존재할 수 있다.
- Level-5가 해당 사용자를 자기 부서에 배정할 수 있다.
- 배정된 계급이 문서 검토 권한에 즉시 반영된다.
- 다른 부서 Level-5의 요청은 `403 Forbidden`이 된다.
- 직원 관리 Service와 Controller 테스트가 통과한다.

---

## Phase 2. 문서 작업 목록과 검색

현재 문서 목록은 공개된 문서만 반환한다. React 화면을 만들기 전에 작성자와 검토자용 목록 API가 필요하다.

### 2-1. 문서 작업 목록

- [ ] 내가 작성한 초안, 반려, 검토 중 문서 목록
- [ ] 같은 부서 Level-4 이상의 `PENDING_REVIEW` 목록
- [ ] Level-5의 최종 승인 대상 표시
- [ ] 전역 관리자의 미배정 문서 검토 목록
- [ ] 문서 상세 조회에도 작성자, 부서, 인가등급 검사 유지

예상 API:

```text
GET /api/documents/mine
GET /api/documents/review-queue
```

### 2-2. 검색과 목록 DTO

- [ ] `page`, `size`, `sort` 기반 페이지네이션
- [ ] 제목, 문서 코드, 종류, 상태, 부서, 인가등급 필터
- [ ] 목록용 `DocumentSummaryResponse` 추가
- [ ] 전체 본문은 상세 응답에서만 반환
- [ ] 기본 정렬을 최근 수정 순으로 통일

### 2-3. 문서 GET 보안 정리

- [ ] 현재 공개 규칙인 `GET /api/documents/*` 범위를 좁히기
- [ ] `/mine`, `/review-queue`가 공개 경로로 잡히지 않게 처리
- [ ] 공개 완료 문서 목록과 공개 상세만 비로그인 허용
- [ ] 개인 목록과 결재 목록은 반드시 로그인 요구

### Phase 2 완료 조건

- 작성자가 작업 중인 모든 자기 문서를 찾을 수 있다.
- Level-4와 Level-5가 현재 소속 부서의 검토 대기 문서만 확인한다.
- 비로그인 사용자는 작업 목록에 접근할 수 없다.
- 검색, 페이지네이션, 권한 테스트가 통과한다.

---

# React 시작 지점

Phase 1과 Phase 2의 백엔드 테스트가 통과하면 React를 시작한다. 고급 백엔드 기능이 전부 끝날 때까지 기다리지 않는다.

이 시점에는 첫 화면 구현에 필요한 API가 준비된다.

- 로그인, 회원가입, 내 정보
- 공개 문서 목록과 상세
- 문서 작성, 수정, 내 문서
- 검토 대기와 승인 대기 목록
- 부서 직원 관리

이후부터는 아래 순서로 기능 하나를 끝까지 완성한다.

```text
Flyway
-> Entity / Repository
-> Service 권한
-> Controller / DTO
-> 백엔드 테스트
-> React API 함수 / 타입
-> React 화면 / 상태 처리
-> 실제 사용자 흐름 테스트
```

---

## Phase 3. React MVP

현재 `frontend`는 기본 Vite + TypeScript 템플릿이다. 이 단계에서 React + TypeScript 구조로 전환한다.

### 3-1. React 기반 설정

- [ ] React 설치 및 Vite 설정 변경
- [ ] 라우팅 구성
- [ ] 공통 API 클라이언트와 오류 처리
- [ ] 로그인 상태와 보호 라우트
- [ ] 환경변수 기반 백엔드 주소 설정
- [ ] 로딩, 빈 목록, 검증 오류, 401, 403, 서버 오류 화면

권장 폴더 구조:

```text
src/
  app/
  api/
  components/
  features/auth/
  features/documents/
  features/departments/
  features/personnel/
```

### 3-2. 화면 구현 순서

1. [ ] 로그인과 회원가입
2. [ ] 공통 레이아웃과 내비게이션
3. [ ] 공개 문서 목록과 상세
4. [ ] 문서 작성과 수정
5. [ ] 내 문서 목록
6. [ ] Level-4 검토 대기 조회 및 반려 화면
7. [ ] Level-5 최종 승인 및 반려 화면
8. [ ] Level-5 부서 직원 관리 화면
9. [ ] 전역 관리자 사용자 관리 화면

### Phase 3 완료 조건

- 신규 사용자가 가입하고 로그인할 수 있다.
- 부서 없는 Level-0 사용자가 미배정 문서를 작성할 수 있다.
- 부서 직원이 부서 문서를 작성할 수 있다.
- Level-4가 같은 부서의 검토 대기 문서를 반려할 수 있다.
- Level-5가 같은 부서의 문서를 최종 승인하거나 반려할 수 있다.
- Level-5가 자기 부서의 하위 직원을 관리할 수 있다.
- UI에서 숨긴 기능도 백엔드에서 다시 차단된다.

---

## Phase 4. 결재 이력과 반려 사유

현재는 문서 상태만 바뀌고 누가 처리했는지 남지 않는다.

### 백엔드

- [ ] 문서 결재 이벤트 테이블 추가
- [ ] 처리자, 작업, 이전 상태, 다음 상태, 의견, 처리 시각 저장
- [ ] 반려할 때 사유 입력 필수
- [ ] 제출, 승인, 반려, 보관, 새 버전 생성 기록
- [ ] 작성자와 최종 승인자를 다른 사람으로 강제할지 정책 확정
- [ ] 권한에 맞는 결재 타임라인 조회 API

예상 작업 종류:

```text
SUBMITTED
APPROVED
REJECTED
ARCHIVED
REVISION_CREATED
```

### React

- [ ] 검토 의견과 반려 사유 입력 모달
- [ ] 문서 상세의 결재 타임라인
- [ ] 권한에 따라 처리자와 처리 시각 표시

---

## Phase 5. 문서 버전과 보관

현재 공개 문서를 수정하면 같은 행이 `DRAFT`가 되어 기존 공개본이 목록에서 사라지고 내용도 덮어써진다. 실제 CMS에서는 공개본을 유지한 채 새 수정본을 검토해야 한다.

### 문서 버전

- [ ] `documents`는 문서의 고정 식별자와 공통 보안 정보로 유지
- [ ] `document_revisions`에 버전별 제목, 요약, 본문, 상태 저장
- [ ] 증가하는 버전 번호 저장
- [ ] 새 수정본을 검토하는 동안 기존 공개본 유지
- [ ] 승인된 수정본을 원자적으로 새 공개본으로 전환
- [ ] 버전 목록과 버전 상세 조회
- [ ] 기본 버전 흐름이 안정된 후 롤백 검토

### 보관

- [ ] `ARCHIVED` 전환 API
- [ ] 초안 작성자와 권한 있는 관리자만 보관 가능
- [ ] 완전 삭제 대신 내용과 이력 보존
- [ ] 일반 목록에서 보관 문서 제외
- [ ] 관리자용 보관 문서 필터

### React

- [ ] 현재 버전 번호와 상태 표시
- [ ] 버전 이력 화면
- [ ] 공개본과 작업 중 수정본 구분
- [ ] 보관 확인과 보관 목록

---

## Phase 6. 추가 문서 양식

현재 `DocumentType`은 종류만 구분하고 모든 문서가 제목, 요약, 본문을 공통으로 사용한다.

공통 보안 정보와 결재 흐름은 `documents`에 유지하고, 문서별 전용 필드만 1:1 상세 데이터로 분리한다. 버전 기능이 들어간 뒤에는 전용 데이터도 해당 문서 버전에 연결한다.

### 추가할 문서 후보

- [ ] 일반 기록
- [ ] 결재 요청서
- [ ] 사고 보고서
- [ ] 연구 보고서
- [ ] ERROR 또는 변칙 개체 보고서
- [ ] 사용자가 추가로 설계한 문서 양식

전용 필드 예시:

```text
결재 요청서:
요청 목적, 요청 자원, 완료 기한, 긴급도

사고 보고서:
발생 시각, 장소, 심각도, 관련 인원, 피해 내용

연구 보고서:
연구 분야, 가설, 연구 방법, 결과, 결론

ERROR / 변칙 개체 보고서:
분류, 위험도, 발견 정보, 격리 상태
```

### 설계 규칙

- [ ] 작성자, 부서, 인가등급, 결재 컬럼을 문서 종류마다 중복하지 않기
- [ ] 선택한 `DocumentType`과 전용 요청 DTO 일치 여부 검증
- [ ] 세계관 전용 명칭은 가능한 한 데모 데이터에 두기
- [ ] 문서 종류 하나씩 백엔드와 React를 함께 완성하기

---

## Phase 7. 협업 및 확장 기능

핵심 결재와 버전 구조가 안정된 뒤 구현한다.

- [ ] 첨부파일 크기, 형식, 저장소, 인가등급 검사
- [ ] 문서 검토 댓글 또는 토론
- [ ] 검토 배정, 승인, 반려 알림
- [ ] 즐겨찾기
- [ ] 최근 열람 문서
- [ ] 전문 검색 또는 고급 검색
- [ ] 인쇄 및 내보내기 화면

---

## Phase 8. 배포 준비

### 보안

- [ ] Refresh Token과 로그아웃 정책
- [ ] 비밀번호 변경과 재설정
- [ ] 로그인 요청 제한과 무차별 대입 방어
- [ ] 운영 환경 CORS 설정
- [ ] 계정 정지 또는 권한 변경 후 기존 토큰 처리
- [ ] 고인가등급 문서 열람 감사 로그
- [ ] 필수 환경변수 시작 시 검증

### 백엔드 품질

- [ ] Open Session in View 비활성화 및 트랜잭션 점검
- [ ] 권한 경계별 통합 테스트
- [ ] API 문서화
- [ ] 테스트 프로필과 격리된 테스트 DB
- [ ] 검증 메시지와 응답 필드명 통일
- [ ] 검색 및 작업 목록용 DB 인덱스 점검

### 배포

- [ ] 백엔드와 프론트엔드 Docker 구성
- [ ] 로컬 MySQL 실행 구성
- [ ] 백엔드 테스트와 프론트엔드 빌드 CI
- [ ] 운영 프로필과 배포 문서
- [ ] Level-0, Level-4, Level-5, 관리자 데모 계정

---

## 4. 마일스톤

### Milestone A: 백엔드 운영 핵심

- Phase 1과 Phase 2 완료
- 직원 관리와 문서 작업 목록을 API 도구로 사용할 수 있음

### Milestone B: Full-stack MVP

- Phase 3 완료
- React에서 회원가입부터 문서 공개까지 전체 흐름 동작

### Milestone C: 포트폴리오급 CMS

- Phase 4~6 완료
- 결재 이력, 문서 버전, 보관, 특수 문서 양식 2개 이상 시연

### Milestone D: 배포 가능 버전

- 선택한 Phase 7 기능과 Phase 8 완료
- 자동 빌드, 테스트, 환경 설정, 데모 데이터 문서화

## 5. 바로 다음 작업

Phase 1의 부서 직원 관리 기능을 다음 순서로 시작한다.

1. 소속 관리 Request / Response DTO
2. Level-5 같은 부서 권한을 검사하는 Membership Service
3. Membership Controller API
4. 전역 관리자의 Level-5 부서장 임명 규칙
5. Repository 및 Service 테스트
6. Level-0과 Level-5 테스트 계정으로 실제 API 확인
